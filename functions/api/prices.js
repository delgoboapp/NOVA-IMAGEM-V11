import {readDB,writeDB,verifyToken,json} from './_shared.js';
export async function onRequestGet(context){const user=await verifyToken(context.env,context.request);if(!user)return json({error:'Sessão inválida ou expirada.'},401);try{return json(await readDB(context.env))}catch(e){return json({error:e?.message||'Não foi possível carregar os dados.'},500)}}
export async function onRequestPost(context){const user=await verifyToken(context.env,context.request);if(!user)return json({error:'Sessão inválida ou expirada.'},401);try{const incoming=await context.request.json(),current=await readDB(context.env);if(user.role!=='gestor'){incoming.users=current.users||[];incoming.disabledUsers=current.disabledUsers||[];incoming.priceConfig=current.priceConfig||null;incoming.products=current.products||[];incoming.suppliers=current.suppliers||[];incoming.payables=current.payables||[];incoming.settings=current.settings||{}}const saved=await writeDB(context.env,incoming);return json({ok:true,updatedAt:saved.updatedAt})}catch(e){return json({error:e?.message||'Não foi possível salvar os dados.'},400)}}
export function onRequest(){return json({error:'Método não permitido.'},405)}
